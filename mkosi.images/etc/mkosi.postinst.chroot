#!/bin/bash

mkdir /etc/extension-release.d/
touch /etc/extension-release.d/extension-release.etc
cat >/etc/extension-release.d/extension-release.etc <<EOF
NAME="$NAME"
ID="_any"
ID_LIKE="atomic-$ID"
VERSION="$VERSION"
VERSION_ID="$VERSION_ID"
EOF